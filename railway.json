{
"$schema": "https://railway.com/railway.schema.json",
"build": {
"builder": "NIXPACKS"
},
"deploy": {
"startCommand": "composer install && php artisan config:clear && php artisan cache:clear && php artisan storage:link && php artisan migrate --force && php -S 0.0.0.0:$PORT -t public"
}
}
